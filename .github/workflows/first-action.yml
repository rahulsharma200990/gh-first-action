name: first_worklflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "Hello World"
            echo "Hello rahul"

      - name: Print By
        run: echo "BY Github"
